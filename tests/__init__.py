"""Test package marker.

This file allows importing test helpers (e.g., `from tests.conftest import ...`) in some
environments where the `tests/` directory is not automatically treated as a package.
"""
